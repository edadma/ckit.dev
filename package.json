{
  "name": "apion-dev",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "nodemon",
    "kill": "kill $(lsof -t -i:3000) || true",
    "test": "sbt test"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
