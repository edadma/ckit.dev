{
  "name": "apion-dev",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "nodemon",
    "kill": "kill $(lsof -t -i:3000) || true",
    "test": "sbt test"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "daisyui": "^5.0.50",
    "nodemon": "^3.1.9",
    "tailwindcss": "^4.1.12"
  }
}
